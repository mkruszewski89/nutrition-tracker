<%= render(
partial: 'recipes/partials/controls/recipe',
locals: {
  recipe: @recipe,
  user: @user
}) %>

<h2>Edit</h2>

<%= link_to("Delete #{@recipe.name}", "/#{@recipe.slug}/edit-full", method: :delete, data: {confirm: "Deleted recipes cannot be restored. Are you sure? DEV NOTE: FOOD LOGS?"}) %>

<%= render(
partial: 'recipes/partials/forms/cu_recipe_full',
locals: {
  model: @recipe,
  url: "/#{@recipe.slug}/edit-full",
  method: :patch
}) %>
