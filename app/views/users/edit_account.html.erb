<h1>My Account</h1>

<%= render(
partial: 'users/partials/controls/user',
locals: {
  user: current_user
}) %>

<br><br><%= link_to("Cancel", "/#{@user.slug}/account", method: :get) %>
<br><br><br>

<%= render(
partial: 'users/partials/forms/edit_account',
locals: {
  model: @user,
  url: "/#{@user.slug}/account/edit",
  method: :patch
}) %>

<br><br>
<%= link_to("Delete Account", "/#{@user.slug}/account/edit", method: :delete, data: {confirm: "Deleted accounts cannot be restored. Associated recipes, logs, etc. will also be destroyed. Are you sure?"}) %>
